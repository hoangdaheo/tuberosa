engine-strict=true
# @hoangdaheo scope is published to / installed from GitHub Packages (private repo registry).
# Auth token is NOT stored here — put it in ~/.npmrc or the NODE_AUTH_TOKEN env var (see docs/INSTALL.md).
@hoangdaheo:registry=https://npm.pkg.github.com
