// Placeholder — populated in session domain extraction step.
// Re-exported from src/types.ts during the staged refactor.
export type {
  ReflectionDraft,
  AgentContextDecisionType,
  AgentSessionStatus,
  AgentSessionOutcome,
} from '../types.js';
